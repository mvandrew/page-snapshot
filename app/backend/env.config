# Конфигурация для сохранения снимков страниц
# Путь к папке, куда будут сохраняться данные снимков
SNAPSHOT_STORAGE_PATH=./storage/snapshots

# Альтернативные примеры путей:
# SNAPSHOT_STORAGE_PATH=C:\snapshots
# SNAPSHOT_STORAGE_PATH=/var/snapshots
# SNAPSHOT_STORAGE_PATH=./data/snapshots

# Настройки размера запросов
# Максимальный размер тела HTTP-запроса для приема больших HTML-страниц
# По умолчанию: 50mb (достаточно для большинства веб-страниц)
# Рекомендуемые значения:
# - 10mb - для простых страниц
# - 50mb - для сложных SPA и больших сайтов (по умолчанию)
# - 100mb - для очень больших страниц с медиа-контентом
# - 500mb - для экстремальных случаев
REQUEST_SIZE_LIMIT=50mb
