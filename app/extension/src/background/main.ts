// Background service worker entry point for the extension.
export const setupBackground = () => {
  // TODO: coordinate capturing page snapshots and posting them to the configured endpoint.
};

setupBackground();
