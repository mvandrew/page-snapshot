// Entry point for the popup UI. Implementation will be added in subsequent iterations.
export const initializePopup = () => {
  // TODO: wire up UI controls and trigger background snapshot requests.
};

initializePopup();
